# Kind cluster with 1 control-plane node + 1 worker node.
# Worker node is where all the vulnerable workloads will be scheduled.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
